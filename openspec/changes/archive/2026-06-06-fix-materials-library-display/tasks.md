## 1. 问题确认与规范对齐

- [x] 1.1 复现 Web 管理端素材库“显示错乱”（刷新后顺序变化/目录混排）并记录复现步骤
- [x] 1.2 对齐规范：确认“全部目录”模式下的分组规则（根目录分组、目录升序、目录内文件名升序）

## 2. 后端排序稳定化

- [x] 2.1 调整素材扫描逻辑：递归扫描时对目录项按“目录优先 + 名称升序”排序，确保索引构建顺序稳定
- [x] 2.2 调整 `GET /api/materials` 的 `items` 输出顺序：目录升序（根目录最小值）+ 文件名升序
- [x] 2.3 调整目录展开逻辑（用于素材选择）：展开目录直属图片文件时按文件名升序，保证选择顺序确定性
- [x] 2.4 为 `materials-service` 增加单元测试：覆盖扫描/排序/目录过滤下的稳定输出顺序

## 3. Web UI 分组展示

- [x] 3.1 调整 Web 管理端素材库渲染：在“全部目录”模式下按 directory 分组输出（包含分组标题），每组内网格展示缩略图
- [x] 3.2 调整目录筛选模式：选择某个目录后仅展示一个分组（分组名为所选目录），仍保持文件名升序
- [x] 3.3 为素材库分组渲染增加前端层面的最小化测试或可验证检查点（如有可用测试框架）

## 4. 集成验证

- [x] 4.1 编写变更级集成测试用例文档（curl 命令）：验证 `/api/materials` 返回排序稳定、筛选模式排序一致
- [x] 4.2 运行 `npm test` 并确保通过
