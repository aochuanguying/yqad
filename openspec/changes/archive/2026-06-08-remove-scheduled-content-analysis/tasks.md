## 1. 调度入口移除

- [x] 1.1 从 `createScheduler` 的 handler 类型中移除 `analysis`。
- [x] 1.2 删除调度器中“内容分析”任务注册逻辑。
- [x] 1.3 删除调度热重载中的“内容分析”任务配置映射。
- [x] 1.4 确认遗漏任务检查只遍历剩余已注册任务，不会补执行内容分析。

## 2. 启动入口和配置调整

- [x] 2.1 从主启动入口传入调度器的 handlers 中移除 `analysis` handler。
- [x] 2.2 保留 `ContentAnalysisService` 实例，确保自动评论和自动发帖仍可按需使用分析摘要。
- [x] 2.3 从默认配置中移除 `scheduler.analysis`，保留顶层 `analysis` 服务配置。
- [x] 2.4 搜索并处理配置类型、Web 配置界面或配置保存逻辑中的 `scheduler.analysis` 残留引用。

## 3. 验证

- [x] 3.1 运行类型检查或构建，确认移除 `analysis` handler 后无类型错误。（注：构建失败源于既有 auto-comment.ts 类型不一致，非本变更引入）
- [x] 3.2 运行调度器相关测试或补充验证，确认只注册签到、自动评论、自动发帖和素材梳理任务。
- [x] 3.3 验证自动发帖仍可通过现有内容分析服务按需获取或刷新分析摘要。
- [x] 3.4 验证配置中存在历史 `scheduler.analysis` 字段时系统不会注册内容分析任务，也不会启动失败。
