## ADDED Requirements

### 需求：素材向量基于大模型图片描述
素材向量化必须优先使用大模型（Vision）为图片生成的自然语言描述作为向量文本来源，以保证语义检索质量；描述必须随素材记录持久化。

#### 场景：描述持久化
- **当** 素材整理调用 Vision 生成图片描述
- **那么** 该描述随素材记录一并存入数据库，不被丢弃

#### 场景：向量文本使用描述
- **当** 为素材生成向量
- **那么** 系统使用图片描述（结合标签）作为向量文本，而非仅文件名

#### 场景：无描述时的退化
- **当** 素材没有可用的大模型描述
- **那么** 系统退化为使用文件名与关键词构建向量文本，不报错

#### 场景：写入与重建向量文本一致
- **当** 分别通过常规写入路径和全量重建路径为同一素材生成向量
- **那么** 两者使用一致的向量文本构建逻辑
