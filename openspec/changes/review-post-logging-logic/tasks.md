## 1. 数据库迁移

- [x] 1.1 创建数据库迁移脚本，添加性能指标字段（pipelineTimings、totalDuration、resourceUsage）
- [x] 1.2 创建数据库迁移脚本，添加调试信息字段（errorStack、contextSnapshot、retryHistory）
- [x] 1.3 添加常用查询索引（created_at、task_id、复合索引 status_trigger_type_created_at）
- [ ] 1.4 执行数据库迁移并验证表结构
- [x] 1.5 准备回滚脚本，确保可安全回退

## 2. 类型定义扩展

- [x] 2.1 更新 `src/types/post-logging.ts`，添加新的接口字段定义
- [x] 2.2 更新 MySQL 存储层的 `CreatePostLogInput` 接口
- [x] 2.3 更新 MySQL 存储层的 `PostLog` 接口
- [x] 2.4 添加 PipelineTimings、ContextSnapshot、RetryHistory 等辅助类型

## 3. 存储层改造

- [x] 3.1 更新 `createPostLog` 方法，支持新字段的插入
- [x] 3.2 更新 `updatePostLog` 方法，支持新字段的更新
- [x] 3.3 更新 `getPostLogById` 方法，解析 JSON 字段（pipelineTimings、contextSnapshot、retryHistory）
- [x] 3.4 更新 `queryPostLogs` 方法，支持按性能指标字段筛选和排序
- [ ] 3.5 添加敏感信息过滤函数，脱敏错误堆栈中的敏感数据

## 4. 服务层改造

- [x] 4.1 更新 `post-logging-service.ts` 的 `log` 方法，支持传入性能指标和调试信息
- [x] 4.2 更新 `getStats` 方法，添加性能指标统计（平均耗时、成功率）
- [x] 4.3 添加异步日志记录机制，使用 `setImmediate` 避免阻塞主流程
- [x] 4.4 添加日志记录超时保护（5 秒超时）
- [x] 4.5 添加日志记录失败的降级处理（记录到控制台但不影响主流程）

## 5. Pipeline 埋点实现

- [ ] 5.1 在 `auto-post.ts` 的 `postWithTopic` 方法中添加 Pipeline 开始和结束埋点
- [ ] 5.2 在步骤 1（子方向选择）中添加执行时长记录
- [ ] 5.3 在步骤 2（内容生成）中添加执行时长记录和中间状态记录
- [ ] 5.4 在步骤 3（素材选择）中添加执行时长记录和资源使用记录
- [ ] 5.5 在步骤 4（图片上传）中添加执行时长记录和降级处理记录
- [ ] 5.6 在步骤 5（话题匹配）中添加执行时长记录
- [ ] 5.7 在步骤 6（多样化变换）中添加执行时长记录
- [ ] 5.8 在步骤 7（合规检查）中添加执行时长记录和检查结果
- [ ] 5.9 在步骤 8（发布）中添加执行时长记录和发布结果

## 6. 错误追踪实现

- [ ] 6.1 实现错误堆栈捕获函数，提取完整的调用链
- [ ] 6.2 实现错误分类函数，标识错误类型和严重程度
- [ ] 6.3 实现上下文快照捕获函数，记录错误发生时的关键参数
- [ ] 6.4 实现重试历史记录函数，记录每次重试的详细信息
- [ ] 6.5 在所有 catch 块中添加错误追踪调用
- [ ] 6.6 实现敏感信息脱敏函数（API Token、用户隐私、数据库连接等）

## 7. 监控指标实现

- [ ] 7.1 实现发帖成功率统计函数（按模式、按时间、按失败原因）
- [ ] 7.2 实现发帖耗时统计函数（平均耗时、P50/P90/P99、趋势）
- [ ] 7.3 实现环节转化率统计函数（各环节通过率、降级率、重试成功率）
- [ ] 7.4 实现实时监控指标查询函数（当前小时、今日累计）
- [ ] 7.5 添加异常告警逻辑（成功率低于阈值、耗时超过阈值时记录告警）

## 8. 测试验证

- [ ] 8.1 编写数据库迁移的单元测试
- [ ] 8.2 编写类型定义的 TypeScript 类型检查
- [ ] 8.3 编写存储层方法的单元测试
- [ ] 8.4 编写服务层方法的单元测试
- [ ] 8.5 编写 Pipeline 埋点的集成测试
- [ ] 8.6 编写错误追踪功能的测试
- [ ] 8.7 编写监控指标统计的测试
- [ ] 8.8 执行性能测试，验证异步日志记录不影响主流程性能

## 9. 文档和部署

- [ ] 9.1 更新 API 文档，说明新增的字段和接口
- [ ] 9.2 编写数据库迁移操作手册
- [ ] 9.3 编写监控指标使用说明
- [ ] 9.4 编写故障排查指南（如何使用新日志系统进行问题定位）
- [ ] 9.5 准备部署清单，包括迁移步骤、验证步骤、回滚步骤
- [ ] 9.6 执行灰度发布，监控关键指标
