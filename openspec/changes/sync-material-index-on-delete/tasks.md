## 1. 删除同步链路

- [ ] 1.1 在素材处理相关服务中新增“删除整理后素材”能力，统一完成路径校验、文件删除和结果返回
- [ ] 1.2 在删除流程中同步清理对应的 `manifest` 记录、`info` 文件和索引项，确保 `.materials` 元数据与文件系统一致
- [ ] 1.3 在 Web 素材管理入口接入删除能力，并在删除后返回刷新后的素材结果或明确状态

## 2. 重建索引入口与修复能力

- [ ] 2.1 新增独立的索引检测/修复服务，检查处理后素材文件、`index`、`manifest` 和 `info` 记录的一致性
- [ ] 2.2 新增素材索引重建接口，返回修复结果摘要与错误信息
- [ ] 2.3 在素材页面“整理素材”按钮旁增加“重建索引”按钮，并接入执行中状态、完成提示和列表刷新

## 3. 读取侧一致性兜底

- [ ] 3.1 修改 `materials-service` 的索引读取逻辑，过滤已失效的索引项并记录索引污染日志
- [ ] 3.2 修改 `image-selector` 的索引匹配逻辑，跳过已删除素材，禁止返回失效路径
- [ ] 3.3 在检测到失效索引项时触发索引修复或重建，确保后续读取结果恢复一致

## 4. 验证与回归

- [ ] 4.1 增加或补充针对“通过系统删除整理后素材”的验证，确认素材列表不再返回已删除项
- [ ] 4.2 增加或补充针对“手工删除处理后文件”的验证，确认浏览接口会过滤失效索引项
- [ ] 4.3 增加或补充针对“点击重建索引按钮”的验证，确认系统完成检测修复并刷新页面
- [ ] 4.4 增加或补充针对“AI 选图命中已删除素材”的验证，确认系统返回其他可用素材或空结果
- [ ] 4.5 运行相关测试或手工验证流程，确认删除、重建索引、浏览和 AI 检索链路没有引入回归
