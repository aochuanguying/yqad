## 1. 新增图片下载服务

- [ ] 1.1 在 Web Server 中配置静态文件中间件，映射 `/images` 到 `materials.processedPath`
- [ ] 1.2 添加文件访问权限验证，禁止访问非图片文件
- [ ] 1.3 添加 404 错误处理（文件不存在时）
- [ ] 1.4 测试图片下载功能（本地和远程访问）

## 2. 实现远程发帖 API

- [ ] 2.1 创建 `POST /api/posts/generate` 路由
- [ ] 2.2 实现 Token 鉴权中间件（复用现有 token 验证逻辑）
- [ ] 2.3 实现频率限制中间件（每小时 10 次）
- [ ] 2.4 创建 `POST /api/posts/batch` 异步批量生成路由
- [ ] 2.5 创建 `GET /api/posts/tasks/:id` 任务查询路由
- [ ] 2.6 实现异步任务管理器（内存队列 + 过期清理）
- [ ] 2.7 定义 API 请求和响应的 TypeScript 类型

## 3. 修改 AutoPostService

- [ ] 3.1 重构 `performDailyPosts()` 方法为内部方法，不自动执行
- [ ] 3.2 创建 `generatePostContent()` 公共方法，返回发帖内容而不发布
- [ ] 3.3 修改图片处理逻辑：生成下载 URL 而非上传到奥迪服务器
- [ ] 3.4 修改返回数据结构：包含标题、正文、图片元数据、模式、推荐话题
- [ ] 3.5 保留所有核心逻辑（主题选择、内容生成、图片匹配、精华策略）
- [ ] 3.6 添加配置检查：`post.mode` 为 'api' 时跳过定时任务

## 4. 修改 Scheduler

- [ ] 4.1 修改定时任务触发条件：检查 `post.mode` 配置
- [ ] 4.2 当 `post.mode === 'api'` 时跳过发帖任务
- [ ] 4.3 更新配置文件示例：添加 `post.mode` 配置项

## 5. 图片元数据服务

- [ ] 5.1 创建图片元数据生成工具函数
- [ ] 5.2 实现文件大小计算功能
- [ ] 5.3 实现相对路径到 URL 的转换
- [ ] 5.4 在发帖内容生成时调用元数据生成

## 6. 错误处理和日志

- [ ] 6.1 添加 API 调用日志（带 `api-call` 标签）
- [ ] 6.2 实现统一的错误响应格式
- [ ] 6.3 添加请求参数验证（必填字段、枚举值等）
- [ ] 6.4 添加详细的错误日志记录

## 7. 配置和文档

- [ ] 7.1 更新 `default.yaml`：添加 `post.mode` 配置项
- [ ] 7.2 更新 `default.yaml`：添加图片下载服务配置（baseUrl）
- [ ] 7.3 创建 API 使用文档（请求示例、响应示例）
- [ ] 7.4 更新 README：说明新的发帖模式

## 8. 测试

- [ ] 8.1 编写同步发帖 API 单元测试
- [ ] 8.2 编写异步批量生成 API 单元测试
- [ ] 8.3 编写图片下载服务测试
- [ ] 8.4 编写 Token 鉴权测试
- [ ] 8.5 编写频率限制测试
- [ ] 8.6 端到端测试：模拟手机端调用流程
- [ ] 8.7 测试配置切换（scheduled ↔ api 模式）
