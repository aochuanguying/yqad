## 1. 部署基线

- [x] 1.1 在项目根目录创建主应用 `Dockerfile`，使用 Debian 系 Node LTS 镜像并补齐构建、运行阶段。
- [x] 1.2 在镜像中安装 `sharp` 运行依赖和 HEIC/HEIF 兜底转换工具，并提供可验证的依赖检查命令。
- [x] 1.3 创建根目录 `.dockerignore`，排除本地依赖、日志、构建产物和无关规划文件。
- [x] 1.4 创建适配群晖 Container Manager 的 `docker-compose.yml`，包含 `config`、`data`、`logs` 卷挂载、端口映射和重启策略。
- [x] 1.5 为 Apple Silicon 本机构建补充 `linux/amd64` 目标平台说明或配置，避免错误镜像架构。

## 2. 图片转换兜底

- [x] 2.1 扩展 `materials.processing` 配置结构，加入 HEIC/HEIF 兜底开关、兜底命令和超时配置的默认值。
- [x] 2.2 重构 HEIC/HEIF 转换逻辑，保留 `sharp` 主链路，并在 Linux 环境下接入命令行兜底转换。
- [x] 2.3 确保兜底成功后仍执行旋转与最终 JPEG 质量输出，保持输出路径与元数据更新逻辑一致。
- [x] 2.4 为兜底工具缺失、执行失败和超时场景补充错误处理与日志，确保单文件失败不会中断整轮素材处理。

## 3. 配置与访问链路

- [x] 3.1 补充容器部署所需的示例配置，明确 `web.baseUrl`、素材目录路径和日志目录的推荐写法。
- [x] 3.2 校验图片 URL 生成与 `/images` 静态访问链路在 NAS 外部访问场景下可用。
- [ ] 3.3 确认 token、素材处理结果和日志在容器重建后的持久化行为符合规范。

## 4. 文档与验证

- [x] 4.1 编写群晖 `DS218+ / NAS218+` 部署文档，覆盖本机构建、群晖导入/启动、配置编辑和访问验证步骤。
- [x] 4.2 在文档中加入 HEIC/HEIF 处理验证步骤，包括检查容器内兜底工具、日志和输出 JPEG 的方法。
- [x] 4.3 在文档中加入常见故障排查与最小回滚方案，重点覆盖平台架构错误、`web.baseUrl` 未配置和 HEIC 转换失败。
- [x] 4.4 更新 `README.md` 的 Docker 部署章节，链接到新的群晖部署文档并概述关键配置项。

## 5. 验收

- [ ] 5.1 在本地使用 `linux/amd64` 完成镜像构建并记录验证结果。
- [ ] 5.2 启动容器并验证 Web 界面、配置加载和 `/images` 图片访问链路。
- [ ] 5.3 使用 HEIC/HEIF 样例验证 `sharp` 主链路与命令行兜底链路至少各成功一次或可被明确观测。
- [ ] 5.4 复查 OpenSpec 产出物与实际实现一致后，再进入 `/opsx:apply` 或实施阶段。
