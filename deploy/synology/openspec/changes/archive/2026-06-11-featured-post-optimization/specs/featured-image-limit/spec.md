## 新增需求

### 需求：系统必须设置精华帖图片数量上限
系统必须限制精华帖的图片数量上限为 9 张，确保帖子质量一致性和用户体验。

#### 场景：图片数量符合上限
- **当** 精华候选帖的图片数量小于等于 `featuredPosting.maxImages`（默认 9 张）
- **那么** 评估结果必须判定图片数量符合上限要求

#### 场景：图片数量超过上限
- **当** 精华候选帖的图片数量超过 `featuredPosting.maxImages`（默认 9 张）
- **那么** 评估结果必须为不达标，并包含"图片数量超过上限"的原因
