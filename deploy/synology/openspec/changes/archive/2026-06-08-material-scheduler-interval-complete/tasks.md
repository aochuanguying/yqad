## 1. 修改配置文件

- [x] 1.1 修改 `config/default.yaml` 中的素材整理调度配置
- [x] 1.2 将 `cron`、`randomOffsetMin`、`randomOffsetMax` 替换为 `intervalMinutes: 30`
- [x] 1.3 验证配置文件语法正确

## 2. 修改调度器代码

- [x] 2.1 在 `src/scheduler/index.ts` 中添加 `materialInterval` 属性
- [x] 2.2 修改 `createScheduler` 函数，使用 `setInterval` 实现间隔执行
- [x] 2.3 实现启动时立即执行逻辑
- [x] 2.4 实现防重入检查（检查 `running` 标志）
- [x] 2.5 修改 `stop()` 方法，清除间隔定时器
- [x] 2.6 添加配置迁移逻辑（检测旧字段并迁移）
- [x] 2.7 更新日志记录（启动时立即执行、间隔执行、跳过执行等）

## 3. 修改配置验证

- [x] 3.1 修改 `src/web/services/config-validator.ts` 中的 `scheduler` 验证方法
- [x] 3.2 移除 `cron`、`randomOffsetMin`、`randomOffsetMax` 的验证
- [x] 3.3 添加 `intervalMinutes` 验证（5-1440 之间的整数）
- [x] 3.4 添加 `enabled` 验证（可选，布尔值）
- [x] 3.5 更新验证错误消息

## 4. 修改 Web 界面

- [x] 4.1 修改 `src/web/public/index.html` 中的配置表单
- [x] 4.2 移除 Cron 表达式、最小偏移、最大偏移输入框
- [x] 4.3 添加执行间隔输入框（数字类型，最小值 5，最大值 1440）
- [x] 4.4 添加启用/禁用复选框
- [x] 4.5 添加快速选择按钮组（5, 10, 15, 30, 60 分钟）
- [x] 4.6 添加"保存配置"按钮和加载状态
- [x] 4.7 更新表单验证逻辑
- [x] 4.8 更新配置说明文字
- [x] 4.9 添加配置保存成功提示
- [x] 4.10 添加配置保存失败处理

## 5. 修改配置 API

- [x] 5.1 修改配置读取接口，返回 `intervalMinutes` 字段
- [x] 5.2 修改配置保存接口，支持 `intervalMinutes` 字段
- [x] 5.3 实现配置热重载逻辑（触发调度器重新配置）
- [x] 5.4 添加配置变更日志记录
- [x] 5.5 测试配置 API 的读取和保存

## 6. 更新类型定义

- [x] 6.1 修改 `src/utils/config.ts` 中的 `SchedulerConfig` 接口
- [x] 6.2 添加 `intervalMinutes` 字段定义
- [x] 6.3 移除或标记 `randomOffsetMin/Max` 为可选（向后兼容）
- [x] 6.4 更新配置加载逻辑

## 7. 测试验证

- [x] 7.1 测试启动时立即执行
- [x] 7.2 测试固定间隔执行（等待 2-3 个间隔）
- [x] 7.3 测试防重入机制（模拟长时间运行）
- [x] 7.4 测试配置迁移（使用旧配置启动）
- [x] 7.5 测试停止机制（验证定时器清除）
- [x] 7.6 测试 Web 界面表单
- [x] 7.7 测试 Web 配置保存功能
- [x] 7.8 测试配置热重载
- [x] 7.9 验证日志输出正确

## 8. 文档更新

- [x] 8.1 更新 README.md 中的调度配置说明
- [x] 8.2 添加间隔执行的使用示例
- [x] 8.3 更新配置迁移指南
- [x] 8.4 添加常见问题解答（FAQ）
- [x] 8.5 添加 Web 配置界面使用说明

## 9. 代码审查和归档

- [x] 9.1 运行代码检查工具（lint/typecheck）
- [x] 9.2 审查所有修改的代码
- [x] 9.3 验证所有测试通过
- [x] 9.4 归档变更
